/* pages/demo05/demo05.wxss
不需要主动引入样式文件，
单位response pixel：rpx
设计稿 750px
750px = 750 rpx
1px = 1 rpx

把屏幕改成 375px
375px = 750 rpx
1px = 2rpx

如果不成比例，比如宽度414或未知page
元素宽度100px
page px = 750 rpx
1 px = 750 / page rpx
100px = 100 * 750 / page
利用calc属性计算，不要直接写表达式
*/

view{
    width: 200rpx;
    height: 200rpx;
    font-size: 40rpx;
    background-color: aqua;
    /*不能留空格*/
    width: calc(750rpx*100/375);
}
